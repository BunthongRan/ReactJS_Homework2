export function FormSubmit (){
    

    return (
        <>
            <h1>Welcome</h1>
            <button onClick={() => alert ("Hello, Inline")}>Click Me</button>
            <button onClick={() => alert("Hey, Good Morning")}>Click Me</button>
        </>
    )
}